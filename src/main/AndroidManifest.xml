<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wib.uigarci">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Event"></activity>
        <activity android:name=".Testimony" />
        <activity android:name=".Annoucement" />
        <activity android:name=".ProductInstruction" />
        <activity android:name=".Agent" />
        <activity android:name=".info" />
        <activity android:name=".user" />
        <activity android:name=".Scan" />
        <activity android:name=".Notification" />
        <activity android:name=".dash" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>